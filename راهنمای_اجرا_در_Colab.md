# راهنمای سریع اجرا در Google Colab 🚀

## روش ساده (پیشنهادی) ⭐

### مرحله 1: باز کردن نوت‌بوک
1. به [Google Colab](https://colab.research.google.com/) بروید
2. فایل `Colab_All_in_One.ipynb` را باز کنید
   - یا از منو: File → Upload notebook → فایل را انتخاب کنید

### مرحله 2: آپلود فایل‌های کد
1. در سمت چپ Colab، روی آیکون 📁 (Files) کلیک کنید
2. روی آیکون آپلود (Upload) کلیک کنید
3. **تمام فایل‌های زیر را آپلود کنید:**

```
src/
  ├── __init__.py
  ├── data_harmonization.py
  ├── feature_engineering.py
  ├── digital_twin.py
  ├── optimization.py
  ├── mcdm.py
  └── utils.py

main.py
config.yaml (یا از سلول نوت‌بوک ایجاد می‌شود)
```

### مرحله 3: اجرا
1. سلول اول را اجرا کنید (نصب کتابخانه‌ها) - Shift + Enter
2. سلول‌های بعدی را به ترتیب اجرا کنید
3. منتظر بمانید تا pipeline کامل اجرا شود (ممکن است 10-30 دقیقه طول بکشد)

---

## روش جایگزین: استفاده از Google Drive

### مرحله 1: آپلود به Drive
1. تمام فایل‌های پروژه را در Google Drive آپلود کنید
2. در Colab، کد زیر را اجرا کنید:

```python
from google.colab import drive
drive.mount('/content/drive')

# کپی فایل‌ها از Drive
!cp -r /content/drive/MyDrive/نام_پوشه_شما/* .
```

### مرحله 2: اجرا
```python
!python main.py
```

---

## نکات مهم ⚠️

### 1. استفاده از GPU (برای سرعت بیشتر)
- Runtime → Change runtime type → GPU
- این کار سرعت آموزش مدل را افزایش می‌دهد

### 2. محدودیت زمان
- Colab رایگان: حداکثر 12 ساعت
- اگر اجرا طولانی شد، نتایج را ذخیره کنید

### 3. ذخیره نتایج
```python
# ذخیره در Drive
!cp -r results/ /content/drive/MyDrive/
!cp -r figures/ /content/drive/MyDrive/

# یا دانلود مستقیم
from google.colab import files
files.download('results/optimization_results.json')
```

### 4. کاهش زمان اجرا (اگر خیلی طولانی شد)
در فایل `config.yaml`:
```yaml
optimization:
  population_size: 30  # به جای 50
  n_generations: 50    # به جای 100
```

---

## مشکلات رایج و راه حل 🔧

### مشکل: `ModuleNotFoundError`
**راه حل:** مطمئن شوید تمام فایل‌های `src/` را آپلود کرده‌اید

### مشکل: `FileNotFoundError: config.yaml`
**راه حل:** 
- فایل `config.yaml` را آپلود کنید
- یا سلول ایجاد config.yaml در نوت‌بوک را اجرا کنید

### مشکل: `Out of Memory`
**راه حل:**
- اندازه dataset را کاهش دهید
- `population_size` را در config کاهش دهید
- Runtime → Restart runtime

### مشکل: اجرا خیلی طولانی می‌شود
**راه حل:**
- از GPU استفاده کنید
- پارامترهای optimization را کاهش دهید
- فقط روی بخشی از داده کار کنید

---

## ساختار فایل‌های مورد نیاز 📁

```
پروژه/
├── main.py                    ✅ ضروری
├── config.yaml                ✅ ضروری (یا از نوت‌بوک ایجاد می‌شود)
├── Colab_All_in_One.ipynb     ✅ نوت‌بوک اصلی
└── src/
    ├── __init__.py           ✅ ضروری
    ├── data_harmonization.py ✅ ضروری
    ├── feature_engineering.py ✅ ضروری
    ├── digital_twin.py       ✅ ضروری
    ├── optimization.py       ✅ ضروری
    ├── mcdm.py              ✅ ضروری
    └── utils.py             ✅ ضروری
```

---

## مثال اجرای سریع 💻

```python
# 1. نصب کتابخانه‌ها
!pip install -q numpy pandas scikit-learn scipy matplotlib seaborn pymoo xgboost lightgbm catboost requests joblib tqdm pyyaml

# 2. ایجاد ساختار
import os
os.makedirs('src', exist_ok=True)
os.makedirs('data', exist_ok=True)
os.makedirs('results', exist_ok=True)
os.makedirs('figures', exist_ok=True)

# 3. آپلود فایل‌ها از طریق Files panel

# 4. اجرا
import sys
sys.path.insert(0, '.')
from main import BuildingEnergyOptimizationPipeline

pipeline = BuildingEnergyOptimizationPipeline()
results = pipeline.run_complete_pipeline()
```

---

## زمان تقریبی اجرا ⏱️

- نصب کتابخانه‌ها: 2-3 دقیقه
- دانلود داده: 1-2 دقیقه
- Feature Engineering: 1-2 دقیقه
- آموزش Digital Twin: 5-10 دقیقه
- بهینه‌سازی (NSGA-II): 10-30 دقیقه (بسته به پارامترها)
- **کل زمان: حدود 20-50 دقیقه**

---

## پشتیبانی 💬

اگر مشکلی پیش آمد:
1. ✅ مطمئن شوید تمام فایل‌ها را آپلود کرده‌اید
2. ✅ خطاها را با دقت بخوانید
3. ✅ ساختار پوشه‌ها را چک کنید
4. ✅ Runtime را Restart کنید

**موفق باشید! 🎉**
