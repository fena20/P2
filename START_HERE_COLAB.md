# 🚀 راهنمای ساده اجرا در Google Colab

## ⚡ روش سریع (3 مرحله)

### مرحله 1️⃣: باز کردن Google Colab
1. به این آدرس بروید: https://colab.research.google.com/
2. روی **File** → **Upload notebook** کلیک کنید
3. فایل **`Colab_All_in_One.ipynb`** را انتخاب و آپلود کنید

### مرحله 2️⃣: آپلود فایل‌های کد
1. در سمت چپ صفحه، روی آیکون **📁 Files** کلیک کنید
2. روی آیکون **⬆️ Upload** کلیک کنید
3. **تمام این فایل‌ها را آپلود کنید:**

```
✅ src/__init__.py
✅ src/data_harmonization.py  
✅ src/feature_engineering.py
✅ src/digital_twin.py
✅ src/optimization.py
✅ src/mcdm.py
✅ src/utils.py
✅ main.py
```

**نکته:** فایل‌های `src/` را باید در یک پوشه به نام `src` آپلود کنید.

### مرحله 3️⃣: اجرا
1. در نوت‌بوک، سلول اول را انتخاب کنید
2. **Shift + Enter** بزنید (یا روی دکمه ▶️ کلیک کنید)
3. منتظر بمانید تا کتابخانه‌ها نصب شوند
4. سلول‌های بعدی را یکی یکی اجرا کنید
5. منتظر بمانید تا pipeline کامل شود (20-50 دقیقه)

---

## 📝 روش جایگزین: آپلود همه فایل‌ها با یک کلیک

اگر می‌خواهید همه فایل‌ها را یکجا آپلود کنید:

### روش A: استفاده از Google Drive

1. **همه فایل‌های پروژه را در Google Drive آپلود کنید**
2. در Colab، این کد را اجرا کنید:

```python
from google.colab import drive
drive.mount('/content/drive')

# مسیر پوشه خود را جایگزین کنید
!cp -r /content/drive/MyDrive/نام_پوشه_شما/* .
```

3. سپس:
```python
!python main.py
```

### روش B: Zip کردن و آپلود

1. **همه فایل‌ها را Zip کنید**
2. در Colab آپلود کنید
3. این کد را اجرا کنید:

```python
!unzip your_files.zip
!python main.py
```

---

## ⚙️ تنظیمات اختیاری (برای سرعت بیشتر)

### استفاده از GPU:
1. **Runtime** → **Change runtime type**
2. **Hardware accelerator** را روی **GPU** تنظیم کنید
3. **Save** کنید

### کاهش زمان اجرا:
اگر اجرا خیلی طولانی شد، در فایل `config.yaml` این مقادیر را تغییر دهید:

```yaml
optimization:
  population_size: 30  # به جای 50
  n_generations: 50    # به جای 100
```

---

## 🔍 بررسی مشکلات

### مشکل: فایل پیدا نمی‌شود
**راه حل:** مطمئن شوید همه فایل‌ها را آپلود کرده‌اید

### مشکل: ModuleNotFoundError
**راه حل:** سلول نصب کتابخانه‌ها را دوباره اجرا کنید

### مشکل: Out of Memory
**راه حل:** 
- Runtime → Restart runtime
- یا پارامترهای optimization را کاهش دهید

---

## ✅ چک‌لیست قبل از اجرا

- [ ] فایل `Colab_All_in_One.ipynb` را در Colab باز کرده‌ام
- [ ] تمام فایل‌های `src/*.py` را آپلود کرده‌ام
- [ ] فایل `main.py` را آپلود کرده‌ام
- [ ] پوشه `src` درست ایجاد شده است
- [ ] سلول نصب کتابخانه‌ها را اجرا کرده‌ام

---

## 📊 بعد از اجرا

نتایج در این پوشه‌ها ذخیره می‌شوند:
- `results/` - نتایج بهینه‌سازی
- `figures/` - نمودارهای Pareto Front

برای دانلود:
```python
from google.colab import files
files.download('results/optimization_results.json')
```

---

**موفق باشید! 🎉**

اگر مشکلی پیش آمد، فایل `راهنمای_اجرا_در_Colab.md` را مطالعه کنید.
